### Basic usage

```bash
$ chezmoi add ~/.bashrc
$ chezmoi edit ~/.bashrc
$ chezmoi git add .
$ chezmoi git commit -- -m "Add bashrc file"
$ chezmoi git push

$ chezmoi init <username|repo>
$ chezmoi apply
$ chezmoi update
```
